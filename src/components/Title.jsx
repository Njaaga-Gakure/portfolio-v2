const Title = ({ name }) => {
  return (
    <div className="center-container title">
      <hr />
      <h3 className="title__text">{name}</h3>
      <hr />
    </div>
  );
};

export default Title;
